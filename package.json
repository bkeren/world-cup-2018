{
  "name": "worldcup2018",
  "version": "1.0.0",
  "description": "FIFA World Cup 2018 REST API",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "start": "nodemon server.js",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../knockout && npm install && npm run build"
  },
  "author": "birand koray eren",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.17.5"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "express": "^4.16.3",
    "node-fetch": "^2.1.2",
    "ora": "^2.1.0",
    "rimraf": "^2.6.2",
    "serve-static": "^1.13.2",
    "shelljs": "^0.8.2",
    "webpack": "^4.15.1",
    "whatwg-fetch": "^2.0.4"
  },
  "engines": {
    "node": "8.11.1"
  }
}
